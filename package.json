{
  "name": "brickshare-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Root workspace for the BrickShare MVP frontend, backend, docs, and tests.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
